{
  "name": "arc-example-ws",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc && npm run copy-cfg",
    "clean": "rm -rf dist",
    "copy-cfg": "find backend -maxdepth 3 -type f \\( -name \"*.arc-config\" -o -name \"package.json\" \\) | cpio -pdm --quiet dist && cp -rf dist/backend/* dist/. && rm -rf dist/backend",
    "watch": "tsc -w",
    "serve": "ARC_TABLES_PORT=5001 arc sandbox",
    "start": "npm run build && concurrently npm:watch npm:serve",
    "deploy:staging": "npm run build && arc deploy",
    "deploy:production": "npm run build && arc deploy production"
  },
  "keywords": [],
  "author": "Brian LeRoux <b@brian.io>",
  "repository": "https://github.com/architect-examples/arc-example-ws",
  "license": "Apache-2.0",
  "devDependencies": {
    "@architect/architect": "^8.4.4",
    "@architect/functions": "^3.13.8",
    "@types/architect__functions": "^3.13.6",
    "@types/node": "^16.9.2",
    "concurrently": "^5.3.0",
    "tslib": "^2.1.0",
    "typescript": "^4.1.3"
  }
}
